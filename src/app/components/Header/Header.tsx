import { Text } from 'react-native';
import * as S from './styles';

export default function Header() {
    return (
        <S.Header>
            <Text>
                Logo
            </Text>
        </S.Header>
    )
}